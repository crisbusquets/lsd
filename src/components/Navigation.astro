---
const { pathname } = Astro.url;
---

<nav>
  <a href="/" class:list={[{ active: pathname === "/" }]}>Joum</a>
  <a href="/quienesel" class:list={[{ active: pathname === "/quienesel" }]}>¿Quién es el él?</a>
  <a href="/docencia" class:list={[{ active: pathname === "/docencia" }]}>Docencia</a>
  <a href="/workshops" class:list={[{ active: pathname === "/workshops" }]}>Workshops</a>
  <a href="/conferencias" class:list={[{ active: pathname === "/conferencias" }]}>Conferencias</a>
  <a href="/publicaciones" class:list={[{ active: pathname === "/publicaciones" }]}>Publicaciones</a>
</nav>

<style>
  nav {
    display: flex;
    flex-direction: column;
    font-size: var(--font-size-nav-1);
    line-height: 1.3;
  }

  @media (max-width: 768px) {
    nav {
      flex-direction: row;
      font-size: var(--font-size-nav-1-mobile);
    }
  }

  a {
    text-decoration: underline;
    transition: all 0.3s;
    color: var(--text-primary);
    margin: 0 0 var(--p-text-xsmall-margin) 0;
  }

  a:hover {
    text-decoration: none;
  }

  a.active {
    font-style: italic;
    font-weight: bold;
  }
</style>
